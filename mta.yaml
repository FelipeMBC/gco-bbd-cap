_schema-version: 3.3.0
ID: gco-bbd-cap
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: gco-bbd-cap-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: gco-bbd-cap-auth
      - name: gco-bbd-cap-db

  - name: gco-bbd-cap-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: gco-bbd-cap-db

resources:
  - name: gco-bbd-cap-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: gco-bbd-cap-${org}-${space}
        tenant-mode: dedicated
  - name: gco-bbd-cap-db
    type: com.sap.xs.hdi-container
    parameters:
      schema: BIBLIOTECA_DIGITAL
      service: hana
      service-plan: hdi-shared
